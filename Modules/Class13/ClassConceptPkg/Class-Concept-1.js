/**
 * 1. Keeping code in a class is better organized and easy to use.
 * 2. If we write code in a class then we can use the code/functions in another file/class.
 */
/**
 * we create class in Js using class-keyword
 * Every class will have a name, preferrably same as the file-name.
 */
class ClassConcept1 {

    /**
     * Benefits:
     * All code written in a class, we can use it in the same class or in another class/file.
     * We can write code in a class where it should logically present.
     */


    // create variables

    // create functions

    /**
     * In general, a class can have:
     *      1. variables
     *      2. functions
     */
    /**
     * Variables in a class:
     * 1. Glabal Variables / Field Variables / Class Variables
     *      are created outside ALL functions of the class
     * 2. Local Variables
     *      are created inside the functions of the class
     */

    /**
     * Class is a representation of a generic entity from real-life.
     * Object of a class represent a particular instance of the class.
     * 
     * Syntax to create object of a class:
     * let objName = new ClassName(); 
     * 
     * new ClassName(); --> code to create class object
     * objName --> variable in which we are storing the class object.
     * 
     * 
     * 
     * Student
     *  attributes (Variables): ->
     *          name
     *          age
     *          id
     *          address
     *          gender
     *          completedTopics
     *          grade
     *      
     *  action (Functions): -> 
     *          classParticipation
     *          submitHomework
     *          dropFromCourse
     *          requestForTA
     *          changeName
     *          updateCompletedTopics
     *          showProfile
     *      
     * student1 is the object of Student-Class
     * 
     * student2 is the object of Student-Class
     */





}